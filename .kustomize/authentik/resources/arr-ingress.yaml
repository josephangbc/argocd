apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: authentik
  name: arr-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: prowlarr.josephang.space
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: authentik-server
            port:
              number: 80
  - host: radarr.josephang.space
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: authentik-server
            port:
              number: 80
  - host: sonarr.josephang.space
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: authentik-server
            port:
              number: 80
  # - host: bazarr.josephang.space
  #   http:
  #     paths:
  #     - path: "/"
  #       pathType: Prefix
  #       backend:
  #         service:
  #           name: authentik
  #           port:
  #             number: 80
